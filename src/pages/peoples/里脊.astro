---
import PersonProfile from '@/components/peoples/PersonProfile.astro'
import { getMemberById } from '@/utils/members'

const person = getMemberById('里脊')

if (!person) {
  return Astro.redirect('/404')
}
---

<PersonProfile person={person} /> 